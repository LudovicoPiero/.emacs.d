# -*- mode: snippet -*-
# name: meta attribute
# key: meta
# --

meta = with lib; {
  description = "$1";
  homepage = "$2";
  license = licenses.$3;
  maintainers = with maintainers; [ ludovicopiero ];
  platforms = platforms.unix;
};